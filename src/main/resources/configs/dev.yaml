# Configuration HTTP
http:
  port: "8080"
  host: "0.0.0.0"
  
# Configuration Sécurité  
api:
  id: "20414799"
  main: "kitinterconnexionuemoa-main"
  key:
    commission: "COMMISSION_SECRET_KEY"

# Configuration Systèmes Externes - CORRECTION: Supprimer les ports explicites
systeme:
  paysA:
    host: "pays-a-simulator.vercel.app"
    port: "443"  # Garder pour la compatibilité mais ne pas utiliser dans l'URL
    url: "https://pays-a-simulator.vercel.app"  # NOUVEAU: URL complète sans port
  paysB:
    host: "pays-b-simulator.vercel.app"
    port: "443"  # Garder pour la compatibilité mais ne pas utiliser dans l'URL
    url: "https://pays-b-simulator.vercel.app"  # NOUVEAU: URL complète sans port

commission:
  uemoa:
    host: "commission-simulator-one.vercel.app"
    port: "443"  # Garder pour la compatibilité mais ne pas utiliser dans l'URL
    url: "https://commission-simulator-one.vercel.app"  # NOUVEAU: URL complète sans port

### database config ####
db:
  driver: "org.h2.Driver"
  url_interco: "jdbc:h2:mem:interconnexion;DB_CLOSE_DELAY=-1"
  user: "sa"
  password: ""
  
oauth:
  client:
    id: "interconnexion_client"
    secret: "interconnexion_secret"

# Configuration Monitoring
monitoring:
  enabled: "true"
logging:
  level: "INFO"